# 🗒️ Внедряем в «Дневник» базу данных

## 🎯 Цель
Готовый проект дневника из Практики 1, в котором **хранилище в памяти заменено на базу данных SQLite через EF Core**.

---

### 1. Функциональность

К моменту сдачи должно работать:

- Страница **списка записей** — данные берутся из БД, а не из словаря/списка в памяти.
- **Создание записи** — запись сохраняется в БД и появляется в списке.
- **Просмотр записи** по `Id` — запись читается из БД.
- **Редактирование записи** — изменения сохраняются в БД, дата создания не меняется.
- После перезапуска приложения **все записи остаются** (данные не пропадают).

Внешний интерфейс (страницы, кнопки, маршруты, сообщения) — такой же, как в Практике 1.

---

### 2. Использование БД и EF Core

В проекте должно быть:

- Настроенная база **SQLite** (файл `app.db` или аналогичный).
- Класс контекста БД, например `AppDbContext` с `DbSet<DiaryEntry>`.
- Модель сущности дневника (например, `DiaryEntry`) с полями:
  - `Id` — целое число, первичный ключ;
  - `Title` — заголовок;
  - `Text` — текст;
  - `CreatedUtc` (или похожее поле) — дата/время создания.

Хранилище в памяти **не используется**:
- нет `static Dictionary`/`static List` для записей;
- нет ручного счётчика `nextId`.

---

### 3. Архитектура и DI

- `AppDbContext` зарегистрирован в DI через `AddDbContext` в `Program.cs`.
- Контроллеры, репозитории и сервисы получают зависимости **через конструктор**, а не через `new`.
- Все операции с записями (список, получение по `Id`, создание, редактирование) идут **через EF Core и БД**.

---

### 4. Критерии проверки

Преподаватель приёмки проверяет, что:

1. Проект собирается и запускается без ошибок.
2. При создании/редактировании записей данные реально попадают в файл БД.
3. После перезапуска приложения записи не пропадают.
4. В коде нет in-memory хранилищ (статических коллекций для записей).
5. Интерфейс и сценарии работы такие же, как в Практике 1, но данные берутся из БД.