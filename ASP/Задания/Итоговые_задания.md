## Практические задания (ASP.NET Core + Razor + SQLite)

### Общие требования для всех заданий:
- Использовать ASP.NET Core с Razor.
- Использовать SQLite для хранения данных.
- Все данные сохраняются в базе данных.

⸻

#### 1) Гостевая книга
- Отзыв содержит: Автор, Текст, ДатаСоздания(UTC).
- Операции: создать, просмотреть, изменить, удалить отзыв.
- Список отзывов:
    - вывод всех отзывов;
    - сортировка: самые новые в начале.

Валидация:
- Автор — обязательное поле;
- Текст — обязательное поле;
- ДатаСоздания создаётся автоматически.
-
Дополнительно:
- добавить пагинацию.

⸻

#### 2) Список дел (Todo)
- Задача содержит: Заголовок, Описание (необязательно), Статус (Выполнена / Не выполнена), ДатаСоздания (UTC).
- Операции: создать, просмотреть, изменить, удалить задачу.
- Список задач:
    - вывод всех задач;
    - сортировка по дате создания (новые в начале).

Валидация:
- Заголовок — обязательное поле;
- Статус — обязательное поле;
- ДатаСоздания создаётся автоматически.

Дополнительно:
- добавить пагинацию.

⸻

#### 3) Каталог товаров
- Товар содержит: Название, Цена, Количество, ДатаДобавления (UTC).
- Операции: создать, просмотреть, изменить, удалить товар.
- Список товаров:
    - вывод всех товаров;
    - сортировка по названию или цене.


Валидация:
- Название — обязательное поле;
- Цена — обязательное поле, больше 0;
- Количество — обязательное поле.

Дополнительно:
- добавить пагинацию.

⸻

#### 4) Контакты
- Контакт содержит: Имя, Телефон, Email (необязательно), Комментарий (необязательно).
- Операции: создать, просмотреть, изменить, удалить контакт.
- Список контактов:
    - вывод всех контактов;
    - сортировка по имени.


Валидация:
- Имя — обязательное поле;
- Телефон — обязательное поле.

Дополнительно:
- добавить пагинацию.

⸻

#### 5) Трекер расходов
- Расход содержит: Категория, Сумма, ДатаРасхода (UTC), Комментарий (необязательно).
- Операции: создать, просмотреть, изменить, удалить расход.
- Список расходов:
    - вывод всех расходов;
    - сортировка по дате (новые в начале).


Валидация:
- Категория — обязательное поле;
- Сумма — обязательное поле, больше 0;
- ДатаРасхода создаётся автоматически.

Дополнительно:
- добавить пагинацию.

⸻

#### 6) Рецепты
- Рецепт содержит: Название, Ингредиенты, ШагиПриготовления, ВремяПриготовленияМин (необязательно).
- Операции: создать, просмотреть, изменить, удалить рецепт.
- Список рецептов:
    - вывод всех рецептов;
    - сортировка по названию.

Валидация:
- Название — обязательное поле;
- Ингредиенты — обязательное поле;
- ШагиПриготовления — обязательное поле.

Дополнительно:
- добавить пагинацию.

⸻

#### 7) Заявки в поддержку
- Заявка содержит: Тема, Описание, Email, Статус (New / InProgress / Closed), ДатаСоздания (UTC).
- Операции: создать, просмотреть, изменить, удалить заявку.
- Список заявок:
    - вывод всех заявок;
    - сортировка по статусу: новые в начале, закрытые в конце.

Валидация:
- Тема — обязательное поле;
- Описание — обязательное поле;
- Статус — обязательное поле;
- ДатаСоздания создаётся автоматически.

Дополнительно:
- добавить пагинацию.

⸻

#### 8) Новости
- Новость содержит: Заголовок, Текст, ДатаПубликации (UTC).
- Операции: создать, просмотреть, изменить, удалить новость.
- Список новостей:
    - вывод всех новостей;
    - сортировка по дате публикации (новые в начале).


Валидация:
- Заголовок — обязательное поле;
- Текст — обязательное поле;
- ДатаПубликации создаётся автоматически.

Дополнительно:
- добавить пагинацию.

⸻

#### 9) Учёт оборудования
- Оборудование содержит: Название, ИнвентарныйНомер, Состояние (Working/Broken), ДатаДобавления (UTC).
- Операции: создать, просмотреть, изменить, удалить оборудование.
- Список оборудования:
    - вывод всего оборудования;
    - сортировка по названию.


Валидация:
- Название — обязательное поле;
- ИнвентарныйНомер — обязательное поле;
- Состояние — обязательное поле.

Дополнительно:
- добавить пагинацию.

⸻

#### 10) Мини- блог
- Пост содержит: Заголовок, Текст, Автор, ДатаСоздания (UTC).
- Операции: создать, просмотреть, изменить, удалить пост.
- Список постов:
    - вывод всех постов;
    - сортировка: самые новые посты в начале.


Валидация:
- Заголовок — обязательное поле;
- Текст — обязательное поле;
- Автор — обязательное поле;
- ДатаСоздания создаётся автоматически.

Дополнительно:
- добавить пагинацию.