# 🗒️ Практическая работа: «Дневник» (ASP.NET Core MVC + Razor)

## 🎯 Цель
Собрать рабочее мини‑приложение «Дневник», в котором можно **создавать**, **просматривать**, **редактировать** записи. Закрепить понимание MVC, маршрутизации, Razor‑форм и валидации.

---

## ✅ Что нужно сделать (ТЗ)
Сделай новое MVC‑приложение и реализовуй следующие возможности:

1) **Список записей дневника**
   - На странице заголовок - Дневник
   - На главной странице отображается нумерованный список всех записей (заголовок, дата). Формат - ***Заголовок записи (Дата)***
   - Каждая запись - ссылка на конкретную запись дневника

2) **Создание записи**
   - Страница формы: поля **Заголовок** и **Текст**.
   - Две кнопки **Сохранить** и **Отмена**.\
    ***Сохранить*** - добавляет запись в Dictionary и выводит алерт ***Запись {Title} сохранена***.\
   **Отменить** - редиректит на список всех записей дневника
   - При успешной отправке — запись добавляется, показывается сообщение об успехе, и происходит переход на список всех записей
   - При ошибках — остаёмся на форме и выводим сообщения валидации под полями.

3) **Просмотр записи**
   - Страница записи дневника должна содержать: заголовок, полный текст записи, дату создания, кнопки **Редактировать** и **Назад к списку**.
   - Если запись не найдена — сообщение ***Записи с идентификатором '{id}' не существует!*** и возврат в список.

4) **Редактирование записи**
   - Страница формы с предзаполненными значениями.
   - Кнока **Сохранить** → переход на список и алерт ***Запись отредактирована {Title}!***.
   - Кнопка **Отмена** возвращает в список без изменений.

5) **Маршрутизация (обязательно единообразная!)**
   - Используй **конвенционный маршрут**:
     `"{controller=Diary}/{action=ListRows}/{id?}"`.
   - Все ссылки/формы генерируй Tag Helper’ами: `asp-controller`, `asp-action`, `asp-route-id`.
   - Параметр `id` должен приходить как сегмент пути (например, `/Diary/EditRow/5`).

6) **Валидация и UX‑поведение**
   - `Text`: обязателен, минимум 5 символов, максимум 2000.
   - Сообщения об ошибках —  алерты


7) **Сообщения пользователю**
   - Для сообщений об успехе/ошибке используй `TempData` и единый блок вывода на страницах.

8) **Хранилище (для этой работы)**
   - Временное хранилище в памяти (например, словарь по `Id`).
   - `Id` назначается автоматически (увеличивается на 1).
   - `Title`: Может быть не заполнен, в этом случае заголовок по умолчанию дата создания.
   - `CreateAt` - заполняется при создании и не меняется после.

9) **Код‑стиль**
   - Разделяй ответственность: модели в `Models/`, контроллер `DiaryController`, представления в `Views/Diary`.
   - Не оставляй мёртвый код и закомментированные блоки без смысла.
   - Названия действий и страниц — осмысленные и единообразные.

---


## 💡 Подсказки ()

### 1) Модели - опиши нужные сущности и добавь валидацию для классов которые принимаешь в POST методах контроллера

```csharp
// Models/DiaryItem.cs (примерный скелет)
public class CreateDiaryModel
{
    // Идентификатор (назначается автоматически)
    public int Id { get; set; }

    ......
}

// Для редактирования можно использовать ту же модель или отдельную
// Обязательно спрошу почему так можно
public class CreteDiaryModel : CreateDiaryModel { }
```

### 2) Хранилище в памяти
```csharp
// Где‑то в контроллере (идея, а не готовое решение)
private static readonly Dictionary<int, DiaryItem> _db = new();
private static int _nextId = 0;

// При создании: var id = _nextId++; _db[id] = item;
```

### 3) Контроллер (жизненный цикл форм)
```csharp
// Controllers/DiaryController.cs (набросок, не копируй дословно)
public class DiaryController : Controller
{
    // Список
    [HttpGet]
    public IActionResult ListRows()
    {
    }

    // Детали
    [HttpGet]
    public IActionResult GetRow(int id)
    {
    }

    // Создание (GET форма)
    [HttpGet]
    public IActionResult CreateRow()
    {
    }

    // Создание (POST)
    [HttpPost]
    [ValidateAntiForgeryToken]
    public IActionResult CreateRow(/* модель */)
    {
    }

    // Редактирование (GET форма с предзаполнением)
    [HttpGet]
    public IActionResult EditRow(int id)
    {
    }

    // Редактирование (POST)
    [HttpPost]
    [ValidateAntiForgeryToken]
    public IActionResult EditRow(int id, /* модель */)
    {
    }
}
```

### 4) Маршрутизация (конвенции)
Обязательно спрошу что это значит
```csharp
// Program.cs
app.MapControllerRoute(
    name: "default",
    pattern: "{controller=Diary}/{action=ListRows}/{id?}");
```

### 5) Razor‑формы c Tag Helpers (скелет)
```cshtml
@model /* твоя модель */

<form asp-action="" method="">
    <div class="mb-3">
        <label asp-for="Title" class="form-label"></label>
        <input asp-for="Title" class="form-control" />
        <span asp-validation-for="Title" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Text" class="form-label"></label>
        <textarea asp-for="Text" class="form-control"></textarea>
        <span asp-validation-for="Text" class="text-danger"></span>
    </div>

    <button type="submit" class="btn btn-primary">Сохранить</button>
    <a asp-action="" class="btn btn-secondary">Отмена</a>
</form>

@section Scripts {
    @* клиентская валидация подключится, если есть _ValidationScriptsPartial *@
    <partial name="_ValidationScriptsPartial" />
}
```

### 6) Ссылки со встроенной генерацией URL
```cshtml
@* В списке *@
<a asp-action="GetRow" asp-route-id="@item.Id">Подробнее</a>
<a asp-action="EditRow" asp-route-id="@item.Id">Редактировать</a>
```

### 7) Сообщения через TempData
```csharp
// После успеха:
TempData["Success"] = "Запись создана!";

// В layout или на странице:
@if (TempData["Success"] is string ok) {
    <div class="alert alert-success">@ok</div>
}
@if (TempData["Error"] is string err) {
    <div class="alert alert-danger">@err</div>
}
```

### 8) Типичные ошибки и как их избежать
- Смешивание конвенций и атрибутов. **Выбери конвенции** и используй `"{controller}/{action}/{id?}"`.
- Отсутствует `{id?}` в шаблоне маршрута → `id` попадает в `?id=...`.
- Нет `asp-validation-for` → ошибки валидации не видны.
- POST без анти‑подделки → 400 Bad Request. Используй `<form asp-action="...">` или `@Html.AntiForgeryToken()`.
- Не проверяешь `ModelState.IsValid` перед сохранением.

### 9) Пара «GET Edit» и «POST Edit»: зачем обе нужны

`GET Edit`

Загружает данные по id.
Заполняет форму начальными значениями (title, text, createDate).
Ничего не меняет в хранилище — просто показывает страницу.

`POST Edit`

Принимает изменённую модель из формы.\
Проверяет **ModelState.IsValid**.\
Обновляет запись и делает Redirect на детали или список.\
Так предотвращаем повторную отправку формы при F5. Это базовый паттерн форм в MVC.

### 10) Дата и время: хранение, вывод, форматирование

#### Что хранить в модели
В учебном проекте достаточно **DateTime CreateDate**.

Для простоты можно использовать **DateTime.Now** - текущее локальное время.

#### Форматирование

Для человека: `ToString("dd.MM.yyyy HH:mm")`.

```cshtml
<time datetime="@Model.CreateDate.ToString("O")">
  @Model.CreateDate.ToString("dd.MM.yyyy HH:mm")
</time>

```

> Правило: в `datetime="..."` кладём ISO (`"O"`), внутри тега — читаемо для человека.

Почему `«в datetime="..." — ISO ("O")»`, а внутри — человекочитаемо

`<time>` — две аудитории сразу

Атрибут `datetime="..."` читают **машины**: скрипты, поисковики, парсеры, сортировки, микроданные.

Содержимое тега читают **люди**.

**Отсюда правило:**

- `datetime`: кладём ISO-8601 «круговой» формат — `ToString("O")`. Он содержит полную дату, доли секунды и **смещение часового пояса** (или `Z` для UTC). Это безошибочный формат для машин и "круговой" при сериализации/парсинге.
- Внутри тега: выводим локально понятный формат, напр. `dd.MM.yyyy HH:mm`.

#### Полезные форматы (стандартные и кастомные)

**Стандартные**

| Формат        | Что это                                     | Пример вывода*                             |
| ------------- | ------------------------------------------- | ------------------------------------------ |
| `"O"` / `"o"` | ISO-8601 «круговой» (с миллисекундами и TZ) | `2025-11-03T14:05:06.1234567+01:00`        |
| `"s"`         | «Сортируемый» ISO без TZ                    | `2025-11-03T14:05:06`                      |
| `"u"`         | Универсальный (UTC, всегда Z)               | `2025-11-03 13:05:06Z`                     |
| `"R"`         | RFC1123 (UTC)                               | `Mon, 03 Nov 2025 13:05:06 GMT`            |
| `"g"` / `"G"` | Краткое/полное дата+время по культуре       | `03.11.2025 14:05` / `03.11.2025 14:05:06` |
| `"d"` / `"D"` | Краткая/длинная дата по культуре            | `03.11.2025` / `3 ноября 2025 г.`          |
| `"t"` / `"T"` | Краткое/полное время по культуре            | `14:05` / `14:05:06`                       |


Кастомные компоненты

- `yyyy` — год, `MM` — месяц, `dd` — день
- `HH` — час (24ч), `hh` — час (12ч) + `tt` (AM/PM) при необходимости
- `mm` — минуты, `ss` — секунды, `fff` — миллисекунды
- `K` — зона (`Z`, `+01:00`, пусто для `Unspecified`)
Примеры:
```cs
date.ToString("dd.MM.yyyy")                  // 03.11.2025
date.ToString("dd.MM.yyyy HH:mm")            // 03.11.2025 14:05
date.ToString("yyyy-MM-ddTHH:mm:ss")         // 2025-11-03T14:05:06
date.ToString("yyyy-MM-ddTHH:mm:ss.fffK")    // 2025-11-03T14:05:06.123+01:00
```

> Частая ошибка: путать `MM` (месяц) и `mm` (минуты).
---

### 11) Почему во View **не надо вручную** заполнять поля значениями модели

> Коротко: используйте `asp-for`/HTML-хелперы. Они **сами** подставляют корректные значения и сообщения об ошибках. Ручной `value="@Model.X"` ломает UX и валидацию.

- В Razor действует приоритет значений: **ModelState → ViewData → Model**.
- При повторном показе формы после ошибок поля должны показывать **то, что ввёл пользователь**, а не старые данные из БД.
- `asp-for` и `Html.*For` **автоматически** берут значение из правильного источника.
- Ручная подстановка `value="@Model.X"` перетирает пользовательский ввод и может нарушить валидацию.

---
#### Как Razor выбирает значение для `<input>`

Когда вы пишете:

```cshtml
<input asp-for="Title" class="form-control" />
<span asp-validation-for="Title" class="text-danger"></span>
```

Razor/Tag Helpers вычисляют `name`, `id`, `value` и привязывают поле к валидации. Значение берётся по правилу:

1. **ModelState (AttemptedValue)** — если форма уже отправлялась и была ошибка, берётся **ввод пользователя**.
2. Если (1) нет — берётся значение из **Model.Property** (то, что вы передали в `View(model)`).
3. Если и (2) нет — значение пустое.


Благодаря этому:
- **GET (Edit)**: показываются исходные данные из модели.
- **POST (ошибка)**: показывается **то, что ввёл пользователь**, + сообщения `asp-validation-for`.
- **POST (успех)**: редирект (PRG), форма не рендерится повторно.
---

#### Антипаттерн: «заполню сам через `value`»

```cshtml
<!-- ПЛОХО: вручную проставили value -->
<input name="Title" value="@Model.Title" class="form-control" />
```

Минусы:
- ❌ **Потеря пользовательского ввода** при ошибке — вы перезатираете значение из ModelState «старыми» данными.
- ❌ **Сломанная привязка** — легко ошибиться с `name`/`id`, и Model Binding не сработает.
- ❌ **Нет автоформатирования/кодирования** — `asp-for` делает это за вас, ручной `value` — нет.
- ❌ **Клиентская валидация** может не сработать (имена/атрибуты не совпадут с моделью).

**Вывод:** не задавайте `value` вручную для полей ввода данных модели.

### Правильный шаблон

### Контроллер
```csharp
// GET: Edit
public IActionResult Edit(int id)
{
    var model = _storage.Get(id);           // загрузили из хранилища
    if (model == null) return NotFound();
    return View(model);                      // на GET поля заполнятся из модели
}

// POST: Edit
[HttpPost]
[ValidateAntiForgeryToken]
public IActionResult Edit(EditModel model)
{
    if (!ModelState.IsValid)
        return View(model);                  // на POST-ошибке покажем ввод пользователя

    _storage.Update(model);                  // сохранить
    TempData["Success"] = "Сохранено";
    return RedirectToAction(nameof(Details), new { id = model.Id }); // PRG
}
```

### View (Razor)
```cshtml
@model EditModel

<form asp-action="Edit" method="post">
  @Html.AntiForgeryToken()
  <input type="hidden" asp-for="Id" />

  <div class="mb-3">
    <label asp-for="Title" class="form-label"></label>
    <input asp-for="Title" class="form-control" />
    <span asp-validation-for="Title" class="text-danger"></span>
  </div>

  <div class="mb-3">
    <label asp-for="Text" class="form-label"></label>
    <textarea asp-for="Text" rows="6" class="form-control"></textarea>
    <span asp-validation-for="Text" class="text-danger"></span>
  </div>

  <button type="submit" class="btn btn-primary">Сохранить</button>
</form>

@section Scripts { <partial name="_ValidationScriptsPartial" /> }
```

---